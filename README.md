# pdf-rag-eval
A production-ready Retrieval-Augmented Generation (RAG) application with built-in retrieval evaluation.

## Setup Instructions
1. `pip install -r requirements.txt`
2. `cp .env.example .env` (Add your Cohere API key)
3. `streamlit run app.py`
